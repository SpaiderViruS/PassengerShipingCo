<Window x:Class="PassengerShipingCo.Windows.CountyList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PassengerShipingCo.Windows"
        mc:Ignorable="d"
        Title="Список стран" Height="450" Width="500" MinHeight="425" MinWidth="475">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <ListView x:Name="CountrysListView" Margin="5" SelectionMode="Single" MouseDoubleClick="CountrysListView_MouseDoubleClick"/>

        <TextBlock x:Name="CountrysPortsTextBlock" Grid.Row="1"/>

        <Grid Grid.Row="2">
            <Button x:Name="ExitBtn" Content="Назад" Click="ExitBtn_Click"/>
        </Grid>
    </Grid>
</Window>
